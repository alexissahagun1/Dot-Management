# DOT Management

Spec: [`docs/SPEC.md`](docs/SPEC.md). Visual lock: `mockups/html/`.

```bash
npm install
cp .env.example .env.local
npm run dev
```

`RESEND_API_KEY` and `RESEND_FROM` are required to send briefings.
